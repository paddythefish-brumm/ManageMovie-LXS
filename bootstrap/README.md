# Bootstrap (0.2.22)

Dieses Verzeichnis enthält nur nicht-sensitive Beispielkonfigurationen für den LXC-Betrieb.

Regeln:
- Keine API-Keys im Repo
- Keine echten Secret-Dateien im Repo
- API-Keys werden beim Erststart in der Web-UI eingetragen

Beispiele:
- `proxmox.env.example`
- `managemovie.env.example`
